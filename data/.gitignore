# Ignore database files but keep the directory
*.db
*.db-wal
*.db-shm
*.lock
backups/*.db

# Ignore admin key file (sensitive!)
.admin_key

# Keep these files
!.gitignore
!.htaccess
!backups/.htaccess
